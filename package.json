{
  "name": "tgestmilhas-scraper",
  "version": "0.1.0",
  "private": true,
  "description": "Serviço de scraping de passagens com milhas (Smiles, Latam Pass, TudoAzul) exposto via HTTP para consumo pelo n8n.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "postinstall": "playwright install --with-deps chromium"
  },
  "dependencies": {
    "dotenv": "17.4.2",
    "express": "^4.19.2",
    "playwright": "^1.47.0"
  }
}
